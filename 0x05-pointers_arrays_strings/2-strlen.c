#include "main.h"

/**
 * swap_int - swaps the values of two integers.
 * @a: pointer to an integer
 * @b: pointer to an integer
 *
 * Return: void
 */
int _strlen(char *s)
{
int i =0;
while (s[i] != '\0')
{
i++;
}
return (i);
}
