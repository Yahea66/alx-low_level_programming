
#include <stdio.h>

/**
* main - prints the alphabet in lowercase, followed by a new line. 
*/
int main(void) 
{
	char alphabet;

	for (alphabet = 'a';alphabet <= 'z';alphabet++)
		putchar(alphabet);

	return (0);
}
